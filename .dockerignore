# Ignore node_modules, build artifacts, and git files
node_modules
**/node_modules
**/dist
**/*.log
.git
.gitignore

# Ignore environment files except production ones
**/.env*
!**/.env.backend.production

# Ignore frontend files (if building only backend)
frontend
!frontend/package.json
# Don't ignore key files
!.env.keys